# TaylorSeer + QwenImage 项目依赖包
# 核心依赖
torch>=2.0.0
diffusers>=0.34.0
transformers>=4.30.0
accelerate>=0.20.0

# 图像处理
Pillow>=9.0.0
numpy>=1.21.0

# NPU 支持 (昇腾 910B)
torch_npu>=2.0.0  # 如果使用 NPU 设备

# 日志和工具
tqdm>=4.60.0
matplotlib>=3.5.0  # 可选，用于可视化

# 开发依赖 (可选)
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
